---
description: エージェントは git add / git commit を実行しない。修正後は差分確認のみ。
alwaysApply: true
---

# Git 自動コミット禁止

- `git add` と `git commit` を実行しない。
- 修正後は変更ファイル一覧と差分の要約を提示し、ユーザーの確認を待つ。
- コミットはユーザーが「コミットして」と明示したときだけ行う。
- 作業完了・品質ゲート通過・`docs/` 生成を、コミット承認と解釈しない。
